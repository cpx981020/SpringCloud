<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenpeixin.cloud.ele.mapper.TypeMapper">

    <select id="query">
        select id,name from t_type where shop_id = #{shopId}
    </select>

</mapper>